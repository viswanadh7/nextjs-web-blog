const Navbar = () => {
    return (
        <nav className="flex justify-between items-center h-16 px-10">
            <h1 className="text-xl">Web blog</h1>
            <button>Login</button>
        </nav>
    );
};

export default Navbar;
